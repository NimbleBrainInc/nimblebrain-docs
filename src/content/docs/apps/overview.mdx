---
title: App Overview
description: A NimbleBrain app is an MCP bundle that exposes tools and optional UI to the agent runtime.
---

import { Aside } from '@astrojs/starlight/components';

A NimbleBrain app is an [MCPB bundle](https://github.com/modelcontextprotocol/mcpb) — a packaged MCP server with a `manifest.json`. Every app exposes **tools** that the agent can call. Apps can optionally provide **UI** rendered inside the NimbleBrain web client as sandboxed iframes, making them full [MCP Apps](/concepts/mcp-apps) compliant with the [ext-apps specification](https://apps.extensions.modelcontextprotocol.io/api/).

## How apps integrate

When you install a bundle, NimbleBrain:

1. Spawns the MCP server as a subprocess (stdio transport) or connects via HTTP (streamable-http/SSE).
2. Discovers the server's tools and registers them in the tool registry.
3. Reads `_meta["ai.nimblebrain/host"]` from `manifest.json` for UI metadata.
4. Registers any UI placements in the shell layout (sidebar items, main views).

The agent accesses tools through the [tiered tool surfacing](/using/managing-apps) system. If the app declares UI metadata, users see the app in the sidebar and can interact with its views directly.

## App types

NimbleBrain recognizes two app types:

| Type | Detection | Description |
|------|-----------|-------------|
| `plain` | Default | Standard MCP server. Tools only, or tools + optional UI via `_meta["ai.nimblebrain/host"]`. |
| `upjack` | `_meta["ai.nimblebrain/upjack"]` present | An [Upjack](https://upjack.dev) declarative app. Automatically gets UI rendering and skill-based behavior. |

The type is stored on the `BundleInstance` and returned in the `GET /v1/apps` response as the `type` field.

## Architecture

```
┌──────────────────────────────────────────────────────┐
│  manifest.json                                       │
│  ├─ server.mcp_config     → how to spawn the server  │
│  └─ _meta["ai.nimblebrain/host"]                 │
│       └─ placements[]     → where UI appears         │
└──────────────┬───────────────────────────────────────┘
               │
               ▼
┌──────────────────────────┐     ┌──────────────────────┐
│  MCP Server (subprocess) │     │  Web Client (shell)  │
│  ├─ Tools                │◄───►│  ├─ Sidebar nav      │
│  └─ ui:// Resources      │     │  └─ Iframe views     │
└──────────────────────────┘     └──────────────────────┘
```

- **Tools** are available to the agent via tiered tool surfacing — directly when the tool count is low, otherwise discovered via `nb__search` (`scope: "tools"`) and invoked through the unified tool namespace.
- **UI resources** are HTML pages served by the MCP server via the `ui://` protocol and proxied through `GET /v1/apps/:name/resources/:path`.
- **The MCP App Bridge** provides a `postMessage` channel between iframes and the host for tool calls, navigation, and data sync.

## Plain MCP bundle (tools only)

The simplest app is a standard MCP bundle with no UI metadata. It exposes tools to the agent and nothing else:

```json title="manifest.json"
{
  "name": "@myorg/weather",
  "version": "1.0.0",
  "server": {
    "type": "python",
    "mcp_config": {
      "command": "python",
      "args": ["-m", "weather.server"]
    }
  }
}
```

## MCP bundle with UI

Add `_meta["ai.nimblebrain/host"]` to give your app a presence in the shell:

```json title="manifest.json"
{
  "name": "@myorg/weather",
  "version": "1.0.0",
  "server": {
    "type": "python",
    "mcp_config": {
      "command": "python",
      "args": ["-m", "weather.server"]
    }
  },
  "_meta": {
    "ai.nimblebrain/host": {
      "host_version": "1.0",
      "name": "Weather",
      "icon": "cloud-sun",
      "primaryView": {
        "resourceUri": "ui://dashboard"
      }
    }
  }
}
```

This registers a main view at `/app/@myorg/weather` and adds a sidebar entry. The `ui://dashboard` resource is an HTML page served by your MCP server and rendered in an iframe.

<Aside type="tip">
You don't need UI to build a useful app. Many powerful apps are tools-only — the agent calls them directly during conversations.
</Aside>

## What's next

- [Manifest Reference](/apps/manifest) — every field in `_meta["ai.nimblebrain/host"]`
- [MCP App Bridge](/apps/bridge) — the postMessage protocol between iframes and the host
- [UI Resources](/apps/ui-resources) — how `ui://` resources are served
- [Placements & Navigation](/apps/placements) — sidebar slots, routes, and priority
- [Publishing to mpak](/apps/publishing) — package and publish your app
- [Hello World Example](/apps/hello-world) — build a complete app from scratch
